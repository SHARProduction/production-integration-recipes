{
  "name": "@shar-production/integration-recipes",
  "version": "1.0.0",
  "description": "Runnable production delivery integration recipes by SHAR Production — https://sharprod.com/",
  "type": "module",
  "scripts": { "test": "node --test", "build": "node scripts/build-site.mjs", "build:distribution": "node scripts/build-distribution.mjs", "check": "npm test && npm run build && node scripts/verify-build.mjs && npm run build:distribution" },
  "author": "SHAR Production (https://sharprod.com/)",
  "license": "MIT",
  "engines": { "node": ">=20" }
}
